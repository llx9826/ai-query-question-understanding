# Wren HTTP 问数集成真实验收方案

更新日期：2026-09-20。状态：目标架构已重新基线为 DuckDB + 标准 Wren Project + 官方 Wren Agent SDK。本文后续出现的 SQLite、自定义 NL2SQL 和 P0–P2 结果仅是历史验证，不能用于新架构放行。新的正式验收按 `plan.md` 的 S0–S5 六个阶段执行，并要求每阶段独立闭环及删除旧问数链路。

本文件配套 [改造计划](plan.md)。验收对象是“管理端上传 Excel → 问数平台发布数据与模型 → HTTP 调用 Wren 通用服务 → Wren 规划并在 SQLite 执行 → 页面展示结果”的完整链路，并验证保留的 PostgreSQL 切换能力。两个项目分别部署，Wren 内核不修改；空间路由、权限和通用 AI 编排在服务封装层实现。飞书只验收扩展边界，不验收接入。

## 1. 真实验收的判定规则

1. 正向自然语言问数必须调用实际模型 API，经真实 HTTP 服务、Wren SDK 规划和新 SQLite 只读适配器返回结果。不以 mock、固定 SQL、预录模型输出、内存替身或旧固定三表 SQLite 链路代替。PostgreSQL 恢复后另做切换回归。
2. 管理端验收必须实际操作浏览器完成上传、观察任务状态和查看答案；HTTP 自动化补充覆盖并发、异常和权限，不能代替管理端验收。
3. SQL 结果与独立参照比较。参照由测试人员根据原始文件使用确定性计算或手写 SQL 生成，不复用被测模型生成的 SQL，也不由模型自评。
4. 允许在隔离测试环境主动断开数据库、停止服务、注入明确故障；这属于故障验收，须标记故障点，不能算正常真实模型成功。
5. 先冻结代码、配置、题库、数据哈希和预期值，再运行。修复后创建新的 run_id，保留先前失败；不得只挑选成功重试生成报告。
6. 状态只允许 PASS、FAIL、BLOCKED、NOT_RUN。没有实际执行或缺关键证据不能标记 PASS；必测项跳过视为未完成。
7. 单元测试和历史验收仅作辅助。旧项目中的 validation-*、历史 README 成绩均不能继承为此次 Wren 集成成绩。

## 2. 开跑条件与凭证

| 条件 | 必须具备的实物或证据 | 本次编写时状态 |
| --- | --- | --- |
| 模型接入 | 确认提供商、HTTPS Base URL、模型 ID；以一次真实调用验证可用性 | 百炼兼容接口已用 `qwen3.7-plus` 与 `qwen3.8-flash` 真实调用通过；未向模型发送真实外宾数据 |
| 问数平台 | 上传、空间鉴权、发布、会话、来源展示均接入新路径 | P1 上传发布及 P2 HTTP 问数入口已实现；浏览器会话切换待 P3 |
| Wren 通用服务 | 可启动 FastAPI 服务，具备模型注册、问数、受限执行及追踪 | 发布注册、自然语言问数、SQL 规划、只读执行、来源和请求幂等已实现；完整可观测性待后续补齐 |
| 数据库 | 首版 A/B 独立 SQLite 发布文件、空间令牌及只读执行；保留 PG 后端 | SQLite P1 真实文件发布和查询已通过；双空间完整矩阵待执行。候选 PG 登录仍被服务端文件权限错误阻断 |
| 运行环境 | 锁定 SDK/驱动版本，记录服务构建标识和数据库版本 | 待锁定 |
| 前端 | 可实际访问的管理页面和业务问数页面 | 新流程待实现 |
| 参照集 | 原文件哈希、测试副本、独立预期结果 | P1 已冻结真实文件哈希及 2340 行、75 列结构；完整题库待 P2 |

API Key 不写入本文件、代码、测试用例、命令行参数、截图或证据包。运行时通过后端环境变量或部署密钥注入；不覆盖现有项目凭证。Key 的字符形态不能用来推断接入域名，不在未确认的第三方地址试用。

运行前确认提供商返回的 usage/request_id 是否可获取；不可获取记 unknown，不能自行编造。记录模型 ID 和非敏感接入地址，不记录 Authorization 头。真实文件的人员明细留在受控测试存储，外部模型只接收查询必要的字段描述、允许的样本或结果。

P1 执行脚本为 `examples/validate_p1_excel_wren.py`，要求显式传入冻结的行列预期并输出脱敏证据。后续阶段仍需交付各自的真实模型、浏览器和隔离验收驱动；缺失时不得宣称对应验收已完成。

### 2.1 已实测的候选 PostgreSQL

用户提供的目标连接（不含密码）：

```text
host: 172.25.200.149
port: 36432
database: level_db
bootstrap_user: postgres
password: 运行时注入，不写入文档或仓库
```

2026-09-20 从当前 Windows 开发主机实测：

- TCP 端口连接成功，首次探测约 15 ms，后续登录前探测约 171 ms。这是端口连接时间，不是 SQL 延迟。
- 使用用户提供的账号密码，通过 pg8000 1.31.5 尝试连接；服务端返回 PostgreSQL FATAL，SQLSTATE `42501`。
- 原始错误：`could not open file "global/pg_filenode.map": Permission denied`。
- 未建立可执行查询的会话，计划中的只读查询未完成。不能据此声称密码验证成功、数据库可用或数据库隔离通过。
- 错误指向数据库进程读取自身文件失败；应检查服务端数据目录属主/权限、容器挂载及安全策略，具体根因尚未确认。未对远端配置或数据做任何修改。

结论：网络连通性 PASS；数据库可用性 BLOCKED。记录为候选环境，不配置为已可用的正式执行连接。修复后从开发机复测登录/只读 SQL，再从实际部署的问数服务与 Wren 容器分别验证网络和凭证。

`postgres` 只作为此次提供的连接身份，尚未查证其实际角色属性。正式 Wren 运行需要独立空间只读角色，不直接沿用该初始化账号；须在隔离的验收 schema 中执行用例，不覆盖 level_db 内现有业务表。

实测证据：[pg-connectivity.json](validation-wren/connectivity-20260920/pg-connectivity.json)。复测工具：[probe_pg_acceptance.py](.local/probe_pg_acceptance.py)，通过隐藏输入读取密码，使用只读事务；驱动仅安装在 `.local/pg-probe-deps/`。后续复测自动使用带时间戳的新证据目录，保留本次失败记录。

## 3. 验收数据与独立预期

### 3.1 当前大会真实 Excel

使用用户指定的原始文件：`C:/Users/00619617/Downloads/2026年国际全球合作伙伴商务峰会 外宾名单.xlsx`。

保留原文件只读，生成测试副本并记录 SHA-256。此前只读检查记录为一个“数据表”sheet、2340 条数据、75 列；正式验收必须重新确定有效行/列口径，不能把历史结构记录当作本次断言。不得直接修改原文件。

制作以下测试副本，所有更改记录在 fixture-manifest.json 中：

- R0：原始快照。
- R1：在 R0 上增加 1 条可识别的合成测试记录、删除 1 条指定记录、修改 1 条其他记录的分组字段。总数相同，用于证明系统确实更新而不是仅比较行数。
- R2：R1 的完全相同副本，用于内容幂等。
- R3：新增普通列；R4：删除或改名一个现有模型引用列；R5：损坏文件。

原文件的人员口径、空值处理、活动选择值以及宽表活动统计规则先写成数据集业务配置并复核。无法明确的规则作为待澄清，不偷偷写入 Wren 通用代码或猜测预期。

### 3.2 双大会同名文件夹具

另外制作两个小型 Excel，分别上传大会 A/B。二者文件名都为“外宾名单.xlsx”，sheet 都为 guests，列名完全相同。内容为合成数据：

| 大会 | guest_id | name | company | country |
| --- | --- | --- | --- | --- |
| A | A001 | 测试同名 | 甲公司 | CN |
| A | A002 | 测试同名 | 乙公司 | US |
| A | A003 | A专属哨兵 | 甲公司 | CN |
| A | A004 | 测试空国家 | 丙公司 | NULL |
| B | B001 | 测试同名 | 丁公司 | DE |
| B | B002 | B专属哨兵 | 丁公司 | FR |
| B | B003 | B普通来宾 | 戊公司 | FR |

冻结预期：A 总数 4、B 总数 3；A 的 CN 为 2、空国家为 1；B 的 FR 为 2；A 的重名查询应保留 A001/A002，不能随意合并。两个专属哨兵用于检出泄漏。

### 3.3 多表与无大会语义夹具

大会 A 再上传两个文件，验证多个 Excel 建模关联：

- events：E01=测试活动一，E02=测试活动二，E03=测试零参与活动。
- participations：(A001,E01)、(A001,E02)、(A002,E01)、(A003,E02)。
- 关系：guest_id 关联 guests；event_id 关联 events。关系需由通用建模流程产生并确认，不能靠测试代码写死查询结果。
- 预期：活动参与记录 4；参与人数去重 3；E01/E02 各 2 人、E03 为 0；A001 参与最多共 2 项；A004 未参与；同时参与 E01/E02 的仅 A001。

再创建独立空间 C，上传不含人员和大会字段的合成订单数据，例如 orders(order_id, customer_id, amount) 和 customers(customer_id, region)。冻结完整内容与分组金额预期，验证引擎代码没有固定三表、品牌或大会分支。

## 4. 必测用例

### 4.1 启动与调用真实性

| ID | 操作 | 通过标准 | 关键证据 |
| --- | --- | --- | --- |
| ENV-01 | 从记录的构建版本启动两个服务和 SQLite 发布卷；PG 切换时启动 PostgreSQL | 服务版本、依赖和数据库状态可查 | 构建标识、版本清单、启动日志 |
| ENV-02 | 通过问数 HTTP 入口提交一个未缓存问题 | 实际模型调用、Wren SDK 规划与执行、真实数据库查询可串联 | 同一 trace_id 的分段日志、模型请求元数据、SQL 及结果摘要 |
| ENV-03 | 在测试部署撤掉旧 SQLite 数据文件和旧建模配置依赖后运行新链路 | 新链路仍能完成上传及问数 | 部署配置、调用链；不能只靠源码关键词搜索 |
| ENV-04 | 检查运行时凭证使用 | 模型 Key 仅后端持有，Wren 使用空间只读角色 | 脱敏配置清单、数据库实际 current_user |

### 4.2 管理端上传、更新和发布

| ID | 操作 | 通过标准 |
| --- | --- | --- |
| UP-01 | 浏览器选 A，首次上传夹具 | 创建数据集，发布后问“总人数”返回 4；来源指向该数据集 |
| UP-02 | 在 B 上传同名文件 | B 返回 3，A 仍返回 4；dataset_id 不跨空间复用 |
| UP-03 | 一次选择多个 Excel 上传 | 各文件有可追踪状态；成功项发布、失败项明确；未形成完整关系的模型不能伪报可用 |
| UP-04 | 上传 R1 更新 R0 数据集 | 保持 dataset_id，产生新 publication_id；新增/删除/修改均生效，总数相同也完成更新 |
| UP-05 | 再上传完全相同内容 R2 | 不重复追加、不创建重复逻辑数据集；允许记录上传审计，不产生无意义的数据发布 |
| UP-06 | 文件改名后显式选择更新原数据集 | 按 dataset_id 更新；仅同名匹配必须局限当前空间，歧义时要求选择 |
| UP-07 | 上传新增列 R3、引用列变更 R4 | 普通新增列按明确规则发布；破坏模型的变更阻止发布或完成校验后的迁移，不能静默错算 |
| UP-08 | 上传损坏文件，或实际制造模型构建失败 | 任务失败可诊断，原已发布数据仍可问，不暴露半成品 |
| UP-09 | 并发上传两个版本，刻意让旧任务后完成 | 最新有效更新不被旧任务覆盖；状态和 active publication 一致 |
| UP-10 | 查询执行中发布新版本 | 单次查询固定一个数据/模型版本；后续新查询读新发布，不混合两版 |
| UP-11 | 回滚发布并再次查询 | 数据、模型及来源一起回到目标版本；运行中的查询可正确完成 |
| UP-12 | 上传失败文件与成功文件后刷新页面、重启 worker | 状态可恢复，重试幂等，不重复发布或显示永远处理中 |

“立即更新”指上传完成后自动启动处理，发布成功后的新查询立即使用新发布版本；不要求文件尚未解析完成就可查询。页面必须区分上传成功、处理中和已可问。

### 4.3 真实模型正确性

先固定至少 20 道核心题，每题注明空间、数据版本、完整问题、是否应澄清以及独立预期。覆盖：

1. 单表总数、条件筛选、分组统计、NULL、空结果、去重、同名不同人。
2. 多文件 JOIN、去重人数与参与记录数区别、零参与活动、零参与人员、并列排名、两个活动交集。
3. 真实 Excel 的活动宽表语义、两个同时满足的条件、结果字段完整性。
4. 连续追问；单对象不明确时澄清；没有签到数据时不能把报名推断成出席。
5. 数据版本更新后的追问明确采用何种版本；无法稳定映射旧结果对象时要求重新查询或澄清。
6. 不相关的订单空间正确完成金额和客户关联统计，不修改通用引擎源码。

核心题执行 3 个独立轮次，每轮使用新会话、禁用答案缓存，至少 60 次核心题请求。所有必答题的结果必须与参照一致；澄清题必须正确澄清且无虚构结果。预先登记的有限自动重试可用，但要保存首次结果、全部尝试和费用/延迟，不掩盖首次失败。

另准备 10 道自然语言改写题，执行 3 轮共 30 次，至少 29 次正确，且错误均可解释；任何跨空间泄漏、虚构来源或把执行失败当作成功均直接阻断验收，不允许被总体准确率抵消。该阈值是本方案的验收要求，不是当前已取得成绩。

对照规则：无排序要求时比较规范化后的行多重集，保留重复次数；有排序时比较顺序与并列口径；NULL 与空串不擅自等同；金额使用十进制精确比较，确需浮点容差时预先记录。核对数据库结果与最终答案，不能 SQL 正确但解释错误也算通过。

### 4.4 大会隔离：任何泄漏均为阻断项

| ID | 实际操作 | 通过标准 |
| --- | --- | --- |
| ISO-01 | A/B 分别问总数与专属哨兵 | 仅返回本空间值；A/B 同名文件、表、模型互不覆盖 |
| ISO-02 | 只有 A 权限的用户篡改请求 workspace_id 为 B | 入口拒绝，既无 B 元数据也无 B 查询结果 |
| ISO-03 | 直接调用 Wren HTTP，伪造空间、过期凭证或缺少认证 | 服务拒绝；不能只在前端做鉴权 |
| ISO-04 | 发送 A 的授权上下文配 B 的模型、发布 ID、数据集 ID 或连接 ID | 任一不匹配均拒绝；服务端绑定连接，不接受任意 DSN |
| ISO-05 | 使用 token_a 请求 workspace_B，并在 A 查询中尝试 ATTACH B 文件；PG 模式再用 reader_a 显式 SELECT B 表 | SQLite 请求与 ATTACH 均拒绝；PG 由权限拒绝并记录 SQLSTATE/current_user。不能仅以返回零行证明隔离 |
| ISO-06 | 在 A 提示模型查询 B、绕过规则、使用全限定 B 表名 | 无泄漏；封装/SDK 拒绝或数据库拒绝，记录实际发生的防线 |
| ISO-07 | 先在 B 产生答案/示例/检索索引，再在 A 提同样问题 | 缓存和知识召回均不含 B；检索不可用的部署记录其实际替代路径 |
| ISO-08 | 在 A 提交 B 的会话、结果、任务 ID 或下载链接 | 不得读取或操作 B；已登录且能访问 A/B 的用户也不能在 A 会话中混用 B 结果 |
| ISO-09 | A/B 交替发起 100 次混合请求，并发度 10；加入服务重连 | 每次查询角色、模型和答案都与空间一致，无全局活动连接串用 |
| ISO-10 | 问题处理中撤销权限，然后读取结果/下载/追问 | 结果交付和后续操作重新鉴权，不再向已撤权用户交付数据 |
| ISO-11 | 请求跨大会汇总 | 默认明确拒绝；不自动遍历空间、不扩展数据库权限 |

数据库查询角色不得是超级用户、业务表所有者或拥有可切换到其他大会角色的成员；检查角色继承、schema USAGE、表 SELECT 和默认权限。schema/search_path 本身不作为安全证据。若公共维表确需共享，必须在冻结的授权清单中声明，其他跨空间读取均失败。

### 4.5 通用执行、故障与运维

| ID | 操作 | 通过标准 |
| --- | --- | --- |
| OPS-01 | 通过正式 SQL 能力提交 INSERT/UPDATE/DELETE/DDL；用查询角色直连尝试写入 | 受测接口拒绝，数据库也无法写；测试数据哈希不变 |
| OPS-02 | 将同一只读 SQL 经 Wren 规划并执行 | 结果与独立 SQL 一致，日志显示确实经过 SDK，来源绑定正确 |
| OPS-03 | 对实际模型调用设置不足的超时，或在测试环境制造模型错误 | 返回明确失败和可追踪原因，不编造结果、不无限重试 |
| OPS-04 | 查询前停止 Wren 或断开数据库，然后恢复 | 问数端明确报告失败；恢复后可再次成功，无跨空间连接回退 |
| OPS-05 | 导入中重启 worker、查询后重启两个服务 | 发布状态和会话可恢复；原文件、模型及数据不丢失 |
| OPS-06 | 制造长查询、超行数结果、取消与重复请求 | 生效的限时/行数/幂等策略可观察；截断必须告知用户 |
| OPS-07 | 冷启动后真实查询，并检查新一轮回归 | Wren 无需复制业务表；仅模型/索引缓存可重建，重启后隔离仍有效 |
| OPS-08 | 查看响应、日志、截图和证据包 | 不含 API Key、数据库密码或其他大会明细；来源含空间、数据集、发布及更新时间 |
| OPS-09 | 检查飞书扩展接口及当前页面 | 预留来源适配契约；不需要飞书凭证，不展示可用但实际未实现的按钮 |

OPS-01 仅在独立测试库执行，不能对生产数据发起破坏性验收。

## 5. 性能与费用记录

首次先测基线，不提前写“性能达标”。至少记录单并发、并发 5 两组；每组至少 30 个真实问数请求，混合简单统计和多表查询，冷启动单列。核心正确性轮次符合条件时可复用，须在报告注明。

记录端到端 P50/P95、各阶段耗时、超时率、数据库耗时、实际模型调用次数和已知 token 用量；失败请求不能从样本中悄悄删除。不得用关闭真实模型后的压测代替端到端成绩。

上传记录文件大小/行列数、解析耗时、建模耗时、发布耗时及处理峰值资源。真实 Excel 和第二类夹具均需测量。性能阈值在首次正式运行前登记；尚未约定时只提交实测值，性能结论标记“未定义达标阈值”，不宣称整体上线验收通过。

费用未知时只记录调用和 usage；如需金额，必须使用运行时确认的提供商计价。开跑前登记最大请求次数、重试上限和费用预算，达到预算停止并保留 BLOCKED/未完成项，不自动无限补跑。

## 6. 每次运行的证据包

输出到 `validation-wren/<run_id>/`。P1 已保留首次失败与修复后的最终通过记录；下列完整证据结构仍是 P2–P4 的目标。

```text
manifest.json             # 代码/构建、依赖、模型、数据哈希、非敏感配置
fixtures/manifest.json    # 测试副本变更与预期；原始个人明细不放公开报告
oracle/results.json      # 独立参照、计算口径、生成脚本标识
cases.jsonl              # 每个用例的全部尝试和结论
traces/                  # 脱敏跨服务追踪、SQL、实际角色和结果摘要
database/                # 授权检查、跨空间拒绝证据与完整性核对
ui/                      # 上传、更新、来源、错误状态截图
metrics.json             # 延迟、调用、usage、资源及样本数量
report.md                # 汇总、失败、阻塞、遗留问题和最终结论
```

每条用例至少记录：case_id、run_id、attempt、开始/结束时间、workspace_id、用户权限场景、dataset/publication/model 标识、trace_id、期望、实测、状态及证据相对路径。请求与答案中的个人信息使用受控原件和脱敏副本分离保存，公开报告仅包含核验必要的摘要。

通过证据必须能串起“页面/HTTP 请求 → 空间授权 → 选定模型/连接 → 真实模型请求 → Wren 规划/SQL → 数据库结果 → 最终答案”。提供商没有请求 ID 时保留本地调用 ID、时间和响应元数据并注明限制。单靠回答看起来合理、服务返回 200 或截图无法证明完整链路。

## 7. 执行顺序与放行标准

1. 完成接入信息确认、环境部署、数据及题库冻结；执行 ENV 和一条单表真实冒烟。
2. 执行 UP、核心正确性、多表及通用订单夹具，保留真实管理端操作证据。
3. 执行所有 ISO 用例和 OPS 故障/恢复用例。
4. 完成三轮真实模型回归、并发与性能记录；核对实际费用及凭证脱敏。
5. 出具报告。修复后使用新 run_id 重跑受影响用例；最终发布候选需完成一套完整必测回归，不能拼接不同构建的局部 PASS 充当最终结果。

放行要求：

- 所有 ENV/UP/ISO/OPS 必测项 PASS；没有未解释 BLOCKED 或 NOT_RUN。
- 核心题三轮全正确，改写题达到阈值；零跨空间数据/元数据泄漏、零未授权写入、零虚构成功。
- 数据新增/更新/回滚、版本一致性及服务重启恢复可重现。
- 前端真实操作、真实模型请求、真实数据库执行与独立预期形成完整证据链。
- 已登记性能阈值并满足；已记录未覆盖的规模边界，不以小夹具结果宣称任意规模可用。
- 报告不带秘密，所有失败与重试可追溯。仅文档、单元测试通过或旧链路通过均不得放行。

## 8. 当前实际结论

P1 后端闭环已经真实执行。`examples/validate_p1_excel_wren.py` 通过管理端 multipart HTTP 上传用户指定的原始 Excel 副本，注册不可变 Wren 发布，并由真实 `WrenEngine.dry_plan()` 规划后在 SQLite 查询。最终 run 验证 1 个 sheet、2340 行、75 列，重复上传不产生新发布，变更上传产生新发布并可回滚原版本，管理响应不暴露内部文件路径。证据见 `validation-wren/p1-real-20260920-r4/acceptance.json`。

首次真实运行因源文件的 XLSX dimension 元数据错误地标成 `A1:A1`，导入器只读出 1 列、0 行；该运行已在 `validation-wren/p1-real-20260920/acceptance.json` 保留为 FAIL。修复后增加对应自动化回归。当前 Wren 服务 8 项及问数项目完整 222 项测试通过；完整回归包含跨项目 P1/P2/P2.1 用例。

本结论放行 P1 后端上传发布能力和 P2 中性数据自然语言问数技术链路。不能据此认定 P3 浏览器管理端、真实外宾数据的外部模型调用或整套上线验收通过。候选 PostgreSQL 仍因服务端文件权限错误阻断。

P2 独立闭环使用真实百炼模型和中性合成 Excel 完成。两轮问题分别经过 SQL 模型、Wren SDK、SQLite 和答案模型，结果均为冻结预期 4；连续追问、来源/发布绑定以及 Wren 服务重启后的幂等重放通过。证据见 `validation-wren/p2-real-neutral-20260920/acceptance.json`。由于真实外宾名单包含个人信息，本次没有把该文件、人员值或聚合结果发送给外部模型；真实名单的 P2 外部模型验收仍为 NOT_RUN。

P2.1 数据边界闭环使用同类中性合成 Excel 和真实百炼 SQL 模型完成。两轮问题均由 Wren 本地执行并得到冻结预期 4；每轮 `model_calls=1`、`answer_model_used=false`、`answer_model_shared_rows=0`，同时通过连续追问、来源/发布绑定和服务重启幂等。证据见 `validation-wren/p2-data-boundary-20260920/acceptance.json`。外部解读开启时的聚合白名单和逐行明细拦截由自动化测试覆盖，不使用真实人员数据。

import json,sqlite3,statistics
from pathlib import Path
D=Path(__file__).parent;ROOT=D.parents[1];OUT=ROOT.parent/'outputs'
scores=json.loads((D/'scores.json').read_text());manifest=json.loads((D/'manifest.json').read_text());results=json.loads((D/'results.json').read_text());spec={c['id']:c for c in manifest['cases']}
notes={
'O01':('775','775','通过'), 'O02':('656，继承 CHERY','656','通过'), 'O03':('75，切换 EXEED 并保留主题大会','75','通过'),
'O04':('英国＋UK：105','105','通过'), 'O05':('14 个活动，名称集合一致','14 个活动，逐项一致','通过'),
'O06':('10 条有效主题大会报名记录；未指定排序方式','返回 10 个正确报名 ID','通过；明细展示偏简'),
'O07':('重名记录 1410、1411，要求选择','正确列出两个候选','行为通过；阶段审计缺失'),
'O08':('交集 1796','1796','通过'), 'O09':('说明实际签到数据缺失，不用计划人数代替','正确说明缺失，但分类为 ambiguous','行为通过；状态契约不通过'),
'O10':('拒绝无关请求，不执行查询','正确拒绝','通过'),
'N01':('并集 999','999','通过'), 'N02':('排除条件，含空白：81','81','通过'),
'N03':('意大利 86、UK 66、Vietnam 66、SPAIN 63、巴西 63','数值、顺序均一致','通过'),
'N04':('160/775，20.65%','20.65%','通过'), 'N05':('公司名称缺失 441','441；文字解释超时','数据通过；完整解释未完成'),
'N06':('4 月 26 日：32','159，误用了 4 月 27 日活动 ID','答案错误'),
'N07':('按文本编号排序的 5 条记录及所指定的 5 列','5 行 5 列逐单元格一致；解释超时','数据通过；完整解释未完成'),
'N08':('4 月 27 日完整日程 10 行及各活动人数','错误要求澄清 OJ 工厂参观日期，未查询','误拦截'),
'N09':('0 条记录，清楚说明无匹配','正确返回空结果','通过'),
'N10':('completed 160、not_required 73、unknown 542','三组及人数一致','通过')}
manual=[]
for c in scores['cases']:
 manual.append({'id':c['id'],'review':'人工逐题核对结果和文字，不调用额外评审API','answer_behavior_pass':c['answer_behavior_pass'],'note':notes[c['id']][2],'narrative_semantically_correct':False if c['id']=='N06' else (True if c['narrative'] else None)})
(D/'manual-review.json').write_text(json.dumps(manual,ensure_ascii=False,indent=2))
lines=['# AI 问数：原 10 题＋随机新增 10 题真实验证','',
'本次答案行为通过 **18/20（90%）**。17 道数据可查询题中，正确回答 **15/17（88.2%）**。按“答案正确、解释完成、状态契约正确、会话保存、审计完整”同时满足的严格口径，闭环通过 **14/20（70%）**。当前还不能判定全部实现。','',
'## 测试口径','',
'- 使用当前网页后端 `/ui-api` → AgentScope 2 原生会话 → 问题理解 → SQL 生成/校验/执行 → 结果呈现的完整入口。通过 FastAPI TestClient 发起真实 ASGI 请求；模型真实调用百炼，数据是真实 Excel 导入的 SQLite。此次没有浏览器、Docker、飞书或 PG 验收。',
'- 保留原 10 道题；O01–O03 在同一会话依次追问，其余独立会话。新增题从 10 个场景组分别随机抽 1 题，随机种子为 20260915，完整候选池和参照 SQL 在模型调用前固定。每题只执行 1 次，未对失败题重试，也未修改业务模块。',
'- 这是分场景随机抽样，不是从生产问题分布中均匀抽样；20 题单轮结果不能外推为所有问题的准确率。',
'- 原始 Excel 有 2340 条报名记录、75 个字段。17 道可查询题的参照答案均已独立从原始 Excel 复核，报名编号、姓名、品牌、国家、公司、职务、入境航班 7 个字段逐记录核对一致。空字符串与未填写按同一缺失语义比较。',
'- 出席标记 1 表示计划出席，不代表实际签到；人数按报名记录计数，不声称是去重自然人数。O07 重名、O09 实际签到缺失、O10 无关请求共 3 题按正确澄清/说明缺失/拒答评分，不记入可查询题准确率。',
'- O06 没有指定排序或展示字段，按返回 10 条真实、互不重复且计划参加主题大会的报名记录评分；当前仅返回报名 ID，数据正确但默认明细展示仍可改善。N07 明确指定字段并进行了逐单元格核对。','',
'| 统计口径 | 原 10 题 | 新 10 题 | 合计 |', '|---|---:|---:|---:|',
'| 正确答案或正确澄清/拒答 | 10/10 | 8/10 | **18/20，90%** |',
'| 可查询题答案正确 | 7/7 | 8/10 | **15/17，88.2%** |',
'| 严格闭环 | 8/10 | 6/10 | **14/20，70%** |','',
'严格闭环额外扣除 O07 的审计缺失、O09 的状态分类错误，以及 N05/N07 的解释超时。解释降级保留了正确数据，因此这两题在“数据答案正确”口径中通过，在“完整解释完成”口径中不通过。','',
'## 逐题结果','',
'| 编号 | 问题 | 独立参照 | 实际结果 | 判定 | 秒 |','|---|---|---|---|---|---:|']
for c in scores['cases']:
 expected,actual,note=notes[c['id']]
 lines.append(f"| {c['id']} | {c['question']} | {expected} | {actual} | {note} | {c['seconds']:.1f} |")
lines+=['','完整原始回答、返回行、SQL 参数、阶段上下文及逐题 trace_id 见证据包。所有 16 次已执行结果均可用保存的 SQL 在原数据库只读重放得到同样结果；N06 也可重放出 159，这说明 SQL 执行成功并不能证明回答符合原问题。','',
'## 问题根因与模块归属','',
'| 优先级 | 归属 | 证据与根因 | 通用调整方向 |', '|---|---|---|---|',
'| P0 | question_understanding：interpret_question / apply_scope_checks / classify_outcome | N06 原话和独立问题都写 4 月 26 日，却输出了 4 月 27 日的 confirmed_entities。当前检查证明 ID 存在、用户说过日期，却没有证明所选 ID 的日期与该条件一致。 | 在理解模块内部完成目录候选过滤和实体绑定。用日期/名称/范围过滤候选后由代码绑定 ID，并检查实体事实与约束一致；候选存在不等于候选匹配。使用现有 Pydantic 契约、目录事实及标准日期处理，不增加模型验证调用。 |',
'| P0 | question_understanding：apply_scope_checks | N08 查询指定日期完整日程，被要求再次澄清 OJ 工厂参观日期。集合查询被当成需要唯一选择某个同名活动，已明确的日期范围未被充分应用。 | 区分集合范围与单实体选择，先应用全局日期范围；只有仍有未确定的选择、且问题要求唯一对象时才澄清。完整日程应把日期范围交给查询模块，不逐活动问用户。 |',
'| P1 | question_understanding：decision_schema / classify_outcome | O09 明确缺少实际签到字段，文字正确，但输出 clarification/ambiguous，而非 unavailable/data_missing。 | 保持缺字段、歧义、能力缺失为不同的判别联合分支；要求本次结构化输出给出缺失数据依据，在本模块确定性校验和归类。不能靠正文关键词让前端修正状态。 |',
'| P1 | 基础设施：query_audit.store / contracts.tracing；不是新增业务模块 | O07 请求表 logging_failed=true，阶段表只有 load_context 与未结束的 begin_turn；随后所有阶段写入被 Trace.write 的短路逻辑跳过。日志仅记录 OperationalError 类型，缺少底层错误码。 | 优先补齐非敏感 SQLite 错误码/操作信息、写入完整性状态，再修复并发写入路径。当前原生异步会话和同步审计共用 SQLite，写锁竞争是疑点，现有日志不足以确认具体 SQLite 错误。复用 SQLAlchemy，避免在事件循环中同步等待写锁；持久化策略独立于模型调用。 |',
'| P1 | answer_presentation：narrate_answer / AgentScopeNarrator | N05/N07 均在 30 秒解释预算超时；SQL 和结果已保存，返回 fallback，未重新生成 SQL。 | 保留现有安全降级和“仅重试解释”的独立能力，单独优化解释模型、输入体量和响应展示；此次未测试重试按钮，也不把更换型号或缩短超时当作已验证提速。 |','',
'N06 的目录和数据本身正确：4 月 26 日活动 `event_75a59c54276a60e569643b9a` 是 32 条；4 月 27 日活动 `event_84ed5e7db843a32bf03e2203` 是 159 条。下游 SQL 按错误的已确认 ID 查询，结果呈现又沿用问题中的日期描述。应先修复上游实体确认，不能把某个题目的正确 ID 硬编码到 SQL 或呈现模块。','',
'本轮没有证据要求修改 source_ingestion、data_preparation 或 semantic_catalog。语义目录已提供两个活动的正确日期与 ID；此次所有数据参照与原表一致。查询模块已有并集、交集、排除、排名、比例、动态明细、空值和空结果能力，但完整日期日程在理解模块就被拦截，本轮未验证其下游执行。','',
'## 速度、调用与保存','',
'- 两个独立会话并行；同一会话的追问串行。20 题平均 44.3 秒，中位数 47.8 秒，最慢 72.3 秒。这是本次并发 2 的观测，不是生产性能承诺。',
'- 普通非空查询为 3 次模型调用：理解 1、SQL 1、解释 1。空结果 2 次；理解阶段拦截 1 次。没有额外模型裁判、模型验证或自动重试。',
'- 已持久化的 model_call 阶段共 50 条：理解 19、SQL 16、解释 15。O07 的 2 次调用阶段因审计故障没有落表；按该题首轮理解路径与 query.model_calls=1 推算全批次为 52 次 SDK 调用。52 是路径推算，50 是数据库实记，不能把漏记的 0 次当作实际未调用；本轮未采集 HTTP 线级请求数。',
'- 已记录的数据库执行 16 次，平均 20.2 毫秒，最大 129 毫秒。模型阶段平均：理解 15.9 秒、SQL 16.7 秒、解释 17.9 秒，仍是主要耗时来源。',
'- 解释发起 15 次，13 次技术上完成、2 次超时降级。N06 虽完成解释，仍然是错误答案。其余已返回解释均已人工与问题、结果逐题核对。',
'- 20/20 回复均标记 context_saved=true，并已从原生会话存储读取到最终消息。本轮没有重启恢复测试。19/20 请求审计完整，O07 明确不完整。',
'- 业务数据库及 app 下业务/编排源码的 SHA-256 前后均未变化；只新增评测脚本和报告。','',
'## 下一步','',
'先单独改 question_understanding，把日期范围、实体选择、歧义与缺数据分类做成可独立验证的节点闭环；重点修复 N06/N08/O09，同时用日期替换、同名活动、日期范围、任意/全部场次与重名人员的变体测试通用规则。优先做代码级校验和候选过滤，不增加模型调用。之后再分别处理审计持久化与结果解释超时，每个模块改完独立验证后再联调。','',
'本次验证没有修复上述问题，也没有为提高通过率重跑失败题。']
report='\n'.join(lines)+'\n'; (OUT/'ai-query-20-real-evaluation.md').write_text(report);(D/'report.md').write_text(report)
